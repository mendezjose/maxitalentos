import React from "react";

const ConsoleButtons = () => {
  return (
    <div>
      <button>Log</button>
      <button>log raw</button>
      <button>clear</button>
    </div>
  );
};

export default ConsoleButtons;
